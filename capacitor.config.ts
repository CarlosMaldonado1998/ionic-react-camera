import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-camera',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
